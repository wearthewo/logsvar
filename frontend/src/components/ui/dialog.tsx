import { Dialog as Primitive } from 'radix-ui'
import { X } from 'lucide-react'
import type { PropsWithChildren, ReactNode } from 'react'
export function Dialog({ open, onOpenChange, title, description, children, trigger }: PropsWithChildren<{ open?: boolean; onOpenChange?: (open:boolean)=>void; title:string; description?:string; trigger?:ReactNode }>) { return <Primitive.Root open={open} onOpenChange={onOpenChange}><Primitive.Trigger asChild>{trigger}</Primitive.Trigger><Primitive.Portal><Primitive.Overlay className="fixed inset-0 z-40 bg-black/70"/><Primitive.Content className="fixed left-1/2 top-1/2 z-50 max-h-[90vh] w-[calc(100%-2rem)] max-w-xl -translate-x-1/2 -translate-y-1/2 overflow-auto rounded-lg border bg-card p-6 shadow-xl"><Primitive.Title className="text-lg font-semibold">{title}</Primitive.Title>{description && <Primitive.Description className="mt-1 text-sm text-muted-foreground">{description}</Primitive.Description>}<div className="mt-5">{children}</div><Primitive.Close className="absolute right-4 top-4 rounded p-1 text-muted-foreground hover:bg-muted" aria-label="Close"><X size={18}/></Primitive.Close></Primitive.Content></Primitive.Portal></Primitive.Root> }
